# 1. Скачиваем установщик Miniconda (если у тебя её еще нет)
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh

# 2. Устанавливаем (жми Enter, читай лицуху пробелом, пиши 'yes')
bash miniconda.sh

# 3. Применяем изменения для текущей сессии терминала
source ~/.bashrc
# (или source ~/.zshrc, если у тебя zsh)

# 4. Создаем изолированную среду (назовем её ctf_sage), ставим туда sage и pwntools
conda create -n ctf_sage -c conda-forge sage pwntools -y

# 5. Активируем нашу новую среду
conda activate ctf_sage




https://ctf.hackthebox.com/event/3296


Алфавіт:  a-z
Текст: kristofer
Ключ 5


https://httpbin.org/json


a3Jpc3RvZmVyX19nb29kX2pvYiEhIQ==


wget https://repo.protonvpn.com/debian/dists/stable/main/binary-all/protonvpn-stable-release_1.0.3-3_all.deb
sudo dpkg -i protonvpn-stable-release_1.0.3-3_all.deb
sudo apt update
sudo apt install protonvpn-cli
protonvpn-cli login
protonvpn-cli c

https://operator.visionaries.team/api/client/conversation?clientid=client_1778096044597_s1knj1o6l

Why are you combining phishing pages into one network? You can jump between them using a dump. Plus, you've implemented automatic checking of read messages; it's not safe.

A: UA575T709V
B: UAQJSNAD9Q
C: UATGA2XP2B


well at least ive dumped all the traffic from here. So if i cant find the source myself, ill pass it on to the appropriate authorities

python3 -c "from pwn import xor; print(xor(bytes.fromhex('0a1709022f3e2c2619343d322b0b36343a3b'), b'CTF').decode())"https://practice.icoa2026.au//register
Логін: kristofer_ua
Пароль: k*fklDd*PvzDsU@C

https://pentesterlab.com/exercises/web-for-pentester-II

#!/usr/bin/env python3
from sage.all import GF, EllipticCurve
import hashlib

p = 2**49 * 3**36 - 1
K = GF(p**2, "x", modulus=[1, 0, 1])
x = K.gen()

def deterministic_sqrt(val):
    return min(val.sqrt(all=True))

def select(lambda0, lambda1, b):
    if b:
        return max(lambda0, lambda1)
    else:
        return min(lambda0, lambda1)

def next_curve(A, alpha, b):
    xi = alpha**2
    zeta = 3 * xi + A
    eta = deterministic_sqrt(zeta)
    new_A = -(4 * A + 15 * xi)
    lambda0 = alpha + 2 * eta
    lambda1 = alpha - 2 * eta
    new_alpha = select(lambda0, lambda1, b)
    return new_A, new_alpha

def explore_forward():
    print("[*] Сканирование аномальных компонент графа...")
    
    # Начинаем с известных суперсингулярных j-инвариантов (0 и 1728)
    base_curves = [
        EllipticCurve(K, [0, 1]),  # j = 0
        EllipticCurve(K, [1, 0])   # j = 1728
    ]
    
    for E_start in base_curves:
        if not E_start.is_supersingular():
            continue
            
        print(f"[*] Анализ кривой с a4={E_start.a4()}, a6={E_start.a6()}")
        
        # Получаем начальные точки кручения
        torsion_points = sorted(E_start.torsion_polynomial(2).roots(multiplicities=False))
        
        for alpha_start in torsion_points:
            for b_init in (0, 1):
                A_curr = E_start.a4()
                alpha_curr = alpha_start
                
                # Ищем короткие циклы или предсказуемое поведение
                history = [(A_curr, alpha_curr)]
                cycle_found = False
                
                for step in range(512):
                    # Эмулируем детерминированный путь (например, всегда берем бит 0 или 1)
                    # В реальном хеше биты псевдослучайны, но структура hex-строки имеет паттерны
                    A_curr, alpha_curr = next_curve(A_curr, alpha_curr, b_init)
                    
                    if (A_curr, alpha_curr) in history:
                        cycle_len = len(history) - history.index((A_curr, alpha_curr))
                        print(f"[+] Найден цикл длины {cycle_len} на шаге {step}!")
                        
                        # Вычисляем финальный хеш этого цикла
                        xi = alpha_curr**2
                        new_A = -(4 * A_curr + 15 * xi)
                        new_B = -alpha_curr * (8 * A_curr + 22 * xi)
                        try:
                            E_final = EllipticCurve(K, [new_A, new_B])
                            target_hash = hashlib.sha256(str(E_final.j_invariant()).encode()).hexdigest()
                            print(f"[!] Возможный credential-отпечаток: {target_hash}")
                        except ArithmeticError:
                            pass
                        
                        cycle_found = True
                        break
                        
                    history.append((A_curr, alpha_curr))
                    
                if cycle_found:
                    continue

if __name__ == "__main__":
    explore_forward()
