# 1. Скачиваем установщик Miniconda (если у тебя её еще нет)
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh

# 2. Устанавливаем (жми Enter, читай лицуху пробелом, пиши 'yes')
bash miniconda.sh

# 3. Применяем изменения для текущей сессии терминала
source ~/.bashrc
# (или source ~/.zshrc, если у тебя zsh)

# 4. Создаем изолированную среду (назовем её ctf_sage), ставим туда sage и pwntools
conda create -n ctf_sage -c conda-forge sage pwntools -y

# 5. Активируем нашу новую среду
conda activate ctf_sage




https://ctf.hackthebox.com/event/3296


Алфавіт:  a-z
Текст: kristofer
Ключ 5


https://httpbin.org/json


a3Jpc3RvZmVyX19nb29kX2pvYiEhIQ==


wget https://repo.protonvpn.com/debian/dists/stable/main/binary-all/protonvpn-stable-release_1.0.3-3_all.deb
sudo dpkg -i protonvpn-stable-release_1.0.3-3_all.deb
sudo apt update
sudo apt install protonvpn-cli
protonvpn-cli login
protonvpn-cli c

https://operator.visionaries.team/api/client/conversation?clientid=client_1778096044597_s1knj1o6l

Why are you combining phishing pages into one network? You can jump between them using a dump. Plus, you've implemented automatic checking of read messages; it's not safe.

A: UA575T709V
B: UAQJSNAD9Q
C: UATGA2XP2B


well at least ive dumped all the traffic from here. So if i cant find the source myself, ill pass it on to the appropriate authorities

python3 -c "from pwn import xor; print(xor(bytes.fromhex('0a1709022f3e2c2619343d322b0b36343a3b'), b'CTF').decode())"https://practice.icoa2026.au//register
Логін: kristofer_ua
Пароль: k*fklDd*PvzDsU@C

https://pentesterlab.com/exercises/web-for-pentester-II

from sage.all import GF, EllipticCurve
from hashlib import sha256
import random

p = 2**49 * 3**36 - 1
K = GF(p**2, "x", modulus=[1, 0, 1])
x = K.gen()

def deterministic_sqrt(val):
    sqrts = val.sqrt(all=True)
    if not sqrts: return None
    return min(sqrts)

def select(lambda0, lambda1, b):
    if b: return max(lambda0, lambda1)
    else: return min(lambda0, lambda1)

def next_curve(A, alpha, b):
    xi = alpha**2
    zeta = 3 * xi + A
    eta = deterministic_sqrt(zeta)
    new_A = -(4 * A + 15 * xi)
    lambda0 = alpha + 2 * eta
    lambda1 = alpha - 2 * eta
    new_alpha = select(lambda0, lambda1, b)
    return new_A, new_alpha

def data_to_bits(data):
    return [int(b) for c in data for b in "{:08b}".format(c)]

def get_predecessors(new_A, new_alpha, b):
    preds = []
    a_c = K(4)
    b_c = K(-2 * new_alpha)
    c_c = K(new_alpha**2 + new_A)
    delta = b_c**2 - 4*a_c*c_c
    
    sqrts = delta.sqrt(all=True)
    for r in sqrts:
        alpha = (-b_c + r) / (2*a_c)
        A = (-new_A - 15*alpha**2) / 4
        
        xi = alpha**2
        zeta = 3 * xi + A
        eta = deterministic_sqrt(zeta)
        if eta is None: continue
        
        test_new_A = -(4 * A + 15 * xi)
        lambda0 = alpha + 2 * eta
        lambda1 = alpha - 2 * eta
        test_new_alpha = select(lambda0, lambda1, b)
        
        if test_new_A == new_A and test_new_alpha == new_alpha:
            preds.append((A, alpha))
    return preds

def get_valid_random_state():
    # Начинаем с известной кривой и делаем случайные шаги для получения валидного узла
    E = EllipticCurve(K, [1, 0])
    A = E.a4()
    alpha = sorted(E.torsion_polynomial(2).roots(multiplicities=False))[1]
    for _ in range(50):
        b = random.randint(0, 1)
        A, alpha = next_curve(A, alpha, b)
    return A, alpha

def CGL_hash(E0, data):
    bits = data_to_bits(data)
    n = len(bits)
    A = E0.a4()
    
    torsion_points = sorted(E0.torsion_polynomial(2).roots(multiplicities=False))
    if bits[0]: alpha = torsion_points[-1]
    else: alpha = torsion_points[1]
        
    for i in range(n - 1):
        A, alpha = next_curve(A, alpha, bits[i + 1])
    xi = alpha**2
    new_A = -(4 * A + 15 * xi)
    new_B = -alpha * (8 * A + 22 * xi)
    E = EllipticCurve(K, [new_A, new_B])
    return sha256(str(E.j_invariant()).encode()).hexdigest()

def solve_step1():
    print("[*] Ищем подходящую стартовую кривую (может занять около минуты)...")
    attempt = 0
    while True:
        attempt += 1
        if attempt % 10 == 0:
            print(f"[*] Попытка {attempt}...")
            
        A_n, alpha_n = get_valid_random_state()
        xi = alpha_n**2
        new_A = -(4 * A_n + 15 * xi)
        new_B = -alpha_n * (8 * A_n + 22 * xi)
        
        try:
            E_t = EllipticCurve(K, [new_A, new_B])
            target_hash = sha256(str(E_t.j_invariant()).encode()).hexdigest()
        except:
            continue
            
        payload = b'"' + target_hash.encode() + b'"'
        bits = data_to_bits(payload)
        
        states = [(A_n, alpha_n)]
        
        died = False
        for i, b in enumerate(reversed(bits[1:])):
            new_states = []
            for A, alpha in states:
                new_states.extend(get_predecessors(A, alpha, b))
            states = list(set(new_states))[:100]
            if not states:
                died = True
                break
                
        if died:
            continue
            
        b0 = bits[0]
        for A, alpha in states:
            a6 = -(alpha**3 + A*alpha)
            try:
                E0 = EllipticCurve(K, [A, a6])
                if not E0.is_supersingular(): continue
                
                torsion_points = sorted(E0.torsion_polynomial(2).roots(multiplicities=False))
                selected_alpha = torsion_points[-1] if b0 else torsion_points[1]
                
                if selected_alpha == alpha:
                    if CGL_hash(E0, payload) == target_hash:
                        print("\n[+] УСПЕХ! Введи эти данные на сервере:")
                        print(f"a4.0 > {A.list()[0]}")
                        print(f"a4.1 > {A.list()[1]}")
                        print(f"a6.0 > {a6.list()[0]}")
                        print(f"a6.1 > {a6.list()[1]}")
                        print("\nchoice > 1")
                        print(f"data (hex) > {payload.hex()}")
                        return E0, payload, target_hash
            except:
                pass

if __name__ == "__main__":
    solve_step1()
