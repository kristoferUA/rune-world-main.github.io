# 1. Скачиваем установщик Miniconda (если у тебя её еще нет)
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh

# 2. Устанавливаем (жми Enter, читай лицуху пробелом, пиши 'yes')
bash miniconda.sh

# 3. Применяем изменения для текущей сессии терминала
source ~/.bashrc
# (или source ~/.zshrc, если у тебя zsh)

# 4. Создаем изолированную среду (назовем её ctf_sage), ставим туда sage и pwntools
conda create -n ctf_sage -c conda-forge sage pwntools -y

# 5. Активируем нашу новую среду
conda activate ctf_sage




https://ctf.hackthebox.com/event/3296


Алфавіт:  a-z
Текст: kristofer
Ключ 5


https://httpbin.org/json


a3Jpc3RvZmVyX19nb29kX2pvYiEhIQ==


wget https://repo.protonvpn.com/debian/dists/stable/main/binary-all/protonvpn-stable-release_1.0.3-3_all.deb
sudo dpkg -i protonvpn-stable-release_1.0.3-3_all.deb
sudo apt update
sudo apt install protonvpn-cli
protonvpn-cli login
protonvpn-cli c

https://operator.visionaries.team/api/client/conversation?clientid=client_1778096044597_s1knj1o6l

Why are you combining phishing pages into one network? You can jump between them using a dump. Plus, you've implemented automatic checking of read messages; it's not safe.

A: UA575T709V
B: UAQJSNAD9Q
C: UATGA2XP2B


well at least ive dumped all the traffic from here. So if i cant find the source myself, ill pass it on to the appropriate authorities

python3 -c "from pwn import xor; print(xor(bytes.fromhex('0a1709022f3e2c2619343d322b0b36343a3b'), b'CTF').decode())"https://practice.icoa2026.au//register
Логін: kristofer_ua
Пароль: k*fklDd*PvzDsU@C

https://pentesterlab.com/exercises/web-for-pentester-II

#!/usr/bin/env python3
from sage.all import GF, EllipticCurve
import hashlib
import sys

p = 2**49 * 3**36 - 1
K = GF(p**2, "x", modulus=[1, 0, 1])
x = K.gen()

def deterministic_sqrt(val):
    return min(val.sqrt(all=True))

def select(lambda0, lambda1, b):
    if b:
        return max(lambda0, lambda1)
    else:
        return min(lambda0, lambda1)

def next_curve(A, alpha, b):
    xi = alpha**2
    zeta = 3 * xi + A
    eta = deterministic_sqrt(zeta)
    new_A = -(4 * A + 15 * xi)
    lambda0 = alpha + 2 * eta
    lambda1 = alpha - 2 * eta
    new_alpha = select(lambda0, lambda1, b)
    return new_A, new_alpha

def data_to_bits(data):
    return [int(b) for c in data.encode() for b in "{:08b}".format(c)]

def explore_backward_dfs():
    print("[*] Запускаем DFS-поиск коллизии (реверс графа с возвратом)...")
    A_target = K(1)
    alpha_target = K(0)
    attempts = 0
    
    while True:
        attempts += 1
        # Делаем псевдослучайный шаг вперед для получения новой целевой кривой
        A_target, alpha_target = next_curve(A_target, alpha_target, attempts % 2)
        
        # Вычисляем параметры E_final и итоговый D (credential)
        xi = alpha_target**2
        new_A = -(4 * A_target + 15 * xi)
        new_B = -alpha_target * (8 * A_target + 22 * xi)
        
        try:
            E_final = EllipticCurve(K, [new_A, new_B])
        except ArithmeticError:
            continue
            
        j_inv = str(E_final.j_invariant()).encode()
        D = hashlib.sha256(j_inv).hexdigest()
        bits = data_to_bits(D)
        
        # Стек для поиска в глубину: (текущий шаг, A, alpha)
        stack = [(511, A_target, alpha_target)]
        
        while stack:
            step, curr_A, curr_alpha = stack.pop()
            
            # Если дошли до начала графа (шаг 0)
            if step == 0:
                a6 = -curr_alpha**3 - curr_A * curr_alpha
                try:
                    E0 = EllipticCurve(K, [curr_A, a6])
                except ArithmeticError:
                    continue
                    
                if not E0.is_supersingular():
                    continue
                    
                # Проверяем строгое условие функции select0
                torsion_points = sorted(E0.torsion_polynomial(2).roots(multiplicities=False))
                b0 = bits[0]
                selected_alpha = torsion_points[-1] if b0 == 1 else torsion_points[1]
                
                if selected_alpha == curr_alpha:
                    a4_coeffs = curr_A.polynomial().list()
                    a6_coeffs = a6.polynomial().list()
                    
                    print(f"\n[+] Успех на попытке {attempts}!")
                    print(f"a4.0 > {a4_coeffs[0] if len(a4_coeffs) > 0 else 0}")
                    print(f"a4.1 > {a4_coeffs[1] if len(a4_coeffs) > 1 else 0}")
                    print(f"a6.0 > {a6_coeffs[0] if len(a6_coeffs) > 0 else 0}")
                    print(f"a6.1 > {a6_coeffs[1] if len(a6_coeffs) > 1 else 0}")
                    print(f"data > {D}")
                    return
                    
            bit = bits[step]
            Delta = -12 * curr_alpha**2 - 16 * curr_A
            
            if Delta.is_square():
                r = Delta.sqrt()
                for sign in (1, -1):
                    cand_alpha = (2 * curr_alpha + sign * r) / 8
                    cand_A = (-curr_A - 15 * cand_alpha**2) / 4
                    
                    # Проверяем валидность прямого шага
                    test_A, test_alpha = next_curve(cand_A, cand_alpha, bit)
                    if test_A == curr_A and test_alpha == curr_alpha:
                        stack.append((step - 1, cand_A, cand_alpha))
        
        if attempts % 50 == 0:
            print(f"[*] Проверено {attempts} целевых кривых...", end="\r")

if __name__ == "__main__":
    explore_backward_dfs()
