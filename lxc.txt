https://ctf.hackthebox.com/event/3296


Алфавіт:  a-z
Текст: kristofer
Ключ 5


https://httpbin.org/json


a3Jpc3RvZmVyX19nb29kX2pvYiEhIQ==


wget https://repo.protonvpn.com/debian/dists/stable/main/binary-all/protonvpn-stable-release_1.0.3-3_all.deb
sudo dpkg -i protonvpn-stable-release_1.0.3-3_all.deb
sudo apt update
sudo apt install protonvpn-cli
protonvpn-cli login
protonvpn-cli c

https://operator.visionaries.team/api/client/conversation?clientid=client_1778096044597_s1knj1o6l

Why are you combining phishing pages into one network? You can jump between them using a dump. Plus, you've implemented automatic checking of read messages; it's not safe.

A: UA575T709V
B: UAQJSNAD9Q
C: UATGA2XP2B


well at least ive dumped all the traffic from here. So if i cant find the source myself, ill pass it on to the appropriate authorities

python3 -c "from pwn import xor; print(xor(bytes.fromhex('0a1709022f3e2c2619343d322b0b36343a3b'), b'CTF').decode())"https://practice.icoa2026.au//register
Логін: kristofer_ua
Пароль: k*fklDd*PvzDsU@C

https://pentesterlab.com/exercises/web-for-pentester-II

from pwn import *

HOST = '154.57.164.63'
PORT = 32073

def get_signature(p, hex_msg):
    p.sendlineafter(b"> ", b"1")
    p.sendlineafter(b"hex: ", hex_msg.encode())
    p.recvuntil(b"Token issued: [")
    token_str = p.recvline().strip().decode()[:-1]
    
    clean_tokens = []
    for x in token_str.split(','):
        val = x.strip().replace('"', '').replace("'", "")
        if val.lower().startswith('0x'):
            val = val[2:]
        # Добиваем нулями до 64 символов, как того требует U256::from_be_hex
        val = val.zfill(64)
        clean_tokens.append(val)
    return clean_tokens

def main():
    p = remote(HOST, PORT)

    print("[*] Вытягиваем 'нулевые' части ключа...")
    zero_keys = get_signature(p, "00" * 32)
    
    print("[*] Вытягиваем 'единичные' части ключа...")
    one_keys = get_signature(p, "ff" * 32)

    print("[*] Формируем подпись для d9_netadmin...")
    target = b"d9_netadmin"
    padded_target = target.rjust(32, b'\x00')

    target_bits = []
    for byte in padded_target:
        for i in range(7, -1, -1):
            target_bits.append((byte >> i) & 1)

    forged_token = []
    for i in range(256):
        if target_bits[i] == 1:
            forged_token.append(one_keys[i])
        else:
            forged_token.append(zero_keys[i])

    print("[*] Отправляем поддельную подпись серверу...")
    p.sendlineafter(b"> ", b"2")
    p.sendlineafter(b"validate: ", target)
    
    payload = ",".join(forged_token)
    p.sendlineafter(b"(comma-separated hex): ", payload.encode())
    
    print("[+] Переход в интерактивный режим. Флаг должен быть здесь:")
    p.interactive()

if __name__ == "__main__":
    main()
